# tbc-lib-go 文件结构说明

> 本文描述当前 `tbc-lib-go` 仓库的主要目录与模块划分，帮助快速了解各功能所在位置。  
> 实际以源码为准，如有新增文件可按下述分组类比理解。

## 顶层目录概览

```
tbc-lib-go/
├── 根目录 (package bt)
│   ├── 交易与 UTXO
│   ├── 区块与网络
│   ├── 脚本与签名哈希
│   ├── 资产 / 池 / API
│   ├── 密钥与加密
│   ├── 通用工具与错误
│   └── 测试文件 *_test.go
│
├── bscript/          # 脚本与地址处理 (package bscript)
├── unlocker/         # 解锁器实现 (package unlocker)
├── sighash/          # 签名哈希标志 (package sighash)
├── taproot/          # Taproot 相关功能 (package taproot)
├── util/             # 工具/辅助组件 (package util 等)
├── cmd/              # 命令行工具 (package main)
├── examples/         # 示例代码
├── testing/          # 测试数据
├── docs/             # 文档（Markdown）
└── 其他工程文件      # go.mod / go.sum / Makefile / LICENSE / README.md 等
```

---

## 根目录：package bt（主包）

所有对外主要能力都集中在根目录的 `package bt` 中，可以大致分为以下几个子模块（按文件前缀/命名来区分）：

### 1. 交易与 UTXO 模块

- `tx.go` / `tx_test.go`  
  交易主结构与构造、序列化、签名等核心逻辑。
- `input.go` / `input_test.go`  
  交易输入结构与相关辅助函数。
- `output.go` / `output_test.go`  
  交易输出结构与相关辅助函数。
- `txinput.go` / `txinput_test.go`  
  输入构建方法（From、解锁相关参数等）。
- `txoutput.go` / `txoutput_test.go`  
  输出构建方法（PayToAddress、找零等）。
- `txchange.go` / `txchange_test.go`  
  找零处理与相关策略。
- `txjson.go` / `txjson_node.go` / `txjson_node_test.go` / `txjson_test.go`  
  交易的 JSON 序列化/反序列化，以及与节点使用格式相关的 JSON 支持。
- `utxo.go`  
  UTXO 结构与相关逻辑。
- `utxojson.go` / `utxojson_test.go`  
  UTXO 的 JSON 表达与转换。

### 2. 区块、网络与消息模块

- `block.go` / `block_test.go`  
  区块结构，区块解析与验证等。
- `blockheader.go`  
  区块头结构与相关操作。
- `merkleblock.go` / `merkleblock_test.go`  
  Merkle Block（简化区块）相关逻辑。
- `message.go` / `message_test.go`  
  P2P 网络消息封装及序列化。
- `network.go` / `network_test.go`  
  网络参数、网络 ID、不同网络（mainnet/testnet 等）的配置。

### 3. 签名、脚本与签名哈希模块

- `signaturehash.go` / `signaturehash_test.go`  
  用于交易签名的签名哈希算法（包含不同哈希类型）。
- `bscript/`（单独包，见下文）  
  提供脚本构造、解析、地址相关操作、脚本解释器等。
- `sighash/flag.go`  
  各种 SIGHASH 标志位常量定义。
- `unlocker.go`  
  定义解锁器接口 `Unlocker` 与 `UnlockerGetter`，由 `unlocker/` 包给出具体实现。

### 4. 资产与池相关 API

这些文件一般用于对外暴露更高层级的资产操作接口：

- `api.go`  
  通用 API 入口或公共类型。
- `api_ft.go`  
  同质化代币（FT）相关 API。
- `api_nft.go`  
  非同质化代币（NFT）相关 API。
- `api_pool.go`  
  池（Pool）相关 API，如资金池、流动性池等能力封装。
- `api_umtxo.go`  
  UMTXO / 特定 UTXO API 包装。
- `api_frozen.go`  
  冻结或相关限制条件下的 API 支持。
- `api_test.go`  
  上述 API 的测试用例。

### 5. 费用计算模块

- `fees.go` / `fees_test.go`  
  交易大小估算、手续费计算与验证逻辑。

### 6. 加密与密钥相关模块

- `ecies.go`  
  ECIES（椭圆曲线集成加密方案）相关实现，用于加解密等。

### 7. 实用工具与通用基础模块

- `varint.go` / `varint_test.go`  
  VarInt 编码/解码实现。
- `bytemanipulation.go` / `bytemanipulation_test.go`  
  通用字节切片操作（拼接、裁剪、反转等）。
- `consts.go`  
  常量定义（网络常量、脚本常量、错误码等）。
- `errors.go`  
  统一错误类型与错误值。
- `docs.go`  
  GoDoc 文档入口（包级注释）。

### 8. 其他

- `Makefile`、`README.md`、`LICENSE`、`go.mod`、`go.sum` 等为工程配置与说明，不属于运行时代码模块。

---

## 子包与子目录说明

### 1. `bscript` 包（脚本与地址）

路径：`bscript/`

主要文件与能力：

- `script.go` / `script_test.go`  
  核心脚本表示与操作。
- `address.go` / `address_test.go`  
  地址生成、解析与校验。
- `addressvalidation.go` / `addressvalidation_test.go`  
  地址验证逻辑抽离。
- `bip276.go` / `bip276_test.go`  
  BIP276 相关实现（例如脚本可读表示等）。
- `unlockingscript.go` / `unlockingscript_test.go`  
  解锁脚本构建与处理。
- `errors.go` / `doc.go`  
  包内错误与文档。
- `opcodes.go` / `oppushdata.go` / `oppushdata_test.go`  
  脚本操作码与 pushdata 相关实现。
- `interpreter/`  
  脚本解释器实现（`engine`、`operations`、`opcodeparser`、测试数据等），用于在本地执行和验证脚本。

### 2. `unlocker` 包（解锁器实现）

路径：`unlocker/`

- `simple.go` / `simple_test.go`  
  一个基于私钥的简单解锁器实现，实现了 `bt` 包中定义的 `Unlocker` 接口，通常用于简单签名场景或示例。

### 3. `sighash` 包（签名哈希标志）

路径：`sighash/`

- `flag.go`  
  各种签名哈希类型（`SIGHASH_ALL`、`SIGHASH_NONE` 等）的标志常量，供 `signaturehash` 及外部调用使用。

### 4. `taproot` 包（Taproot 支持）

路径：`taproot/`

- `taproot.go` / `taproot_test.go` / `alignment_test.go`  
  Taproot 相关数据结构与算法支持，包含对齐、验证等逻辑。

### 5. `util` 目录

路径：`util/`

- 当前包含 `partialsha256/` 等子模块，用于提供特定场景下的辅助工具（如部分哈希计算等），一般作为内部依赖使用。

### 6. `cmd` 目录（命令行工具）

路径：`cmd/`

- `verify-contract/`  
  - `main.go`：合约验证相关命令行工具入口。  
  - `README.md`：命令行使用说明。  
- `verify-interpreter/`  
  - `main.go`：脚本解释器验证工具入口。

这些子命令均以 `package main` 实现，对应可编译为可执行程序。

### 7. `examples` 目录（示例代码）

路径：`examples/`

包含多个示例子目录，例如：

- `create_tx` / `create_tx_with_opreturn`  
  创建基础交易、带 OP_RETURN 输出的交易示例。
- `marshal_node_list` / `marshal_node_tx`  
  与节点格式相关的交易/列表序列化示例。
- `read_txs_from_block` / `sample_transaction` / `transaction_test`  
  从区块读取交易、构造示例交易等。
- `unlocker_getter` / `unmarshal_node_list` / `unmarshal_node_tx`  
  解锁器获取、反序列化节点格式数据等示例。

这些示例展示了包在真实场景下的组合使用方式。

### 8. `testing` 目录（测试数据）

路径：`testing/`

- `data/`  
  二进制区块、交易等测试数据文件，配合测试用例使用，不直接对外暴露 API。

### 9. `docs` 目录（文档）

路径：`docs/`

- `index.md`  
  文档首页，概览与入口。
- `transaction.md` / `unspentoutput.md` / `block.md` / `script.md` / `networks.md` / `ecies.md` 等  
  针对交易、UTXO、区块、脚本、网络配置、加密等主题的详细说明。

---

## 文件命名与模块归类约定

在 `package bt`（根目录）中，所有文件都在同一目录下，但可以通过文件名快速判断所属模块：

- `tx*.go`         → 交易相关（`tx.go`、`txinput.go`、`txoutput.go`、`txchange.go` 等）
- `*input*.go`     → 输入相关
- `*output*.go`    → 输出相关
- `*change*.go`    → 找零相关
- `*json*.go`      → JSON 序列化/反序列化相关
- `signature*.go`  → 签名哈希相关
- `fees*.go`       → 费用计算相关
- `utxo*.go`       → UTXO 结构与 JSON 相关
- `varint*.go`     → VarInt 编码相关
- `*manipulation*.go` → 字节处理工具
- `api_*.go`       → 各类对外 API（FT / NFT / Pool / Frozen / UMTXO 等）

---

## Go 包结构注意事项

- **同一包的所有 Go 文件必须位于同一目录中**，因此 `package bt` 的文件全部位于仓库根目录。
- 独立功能模块通过**子包**形式组织在子目录中，如 `bscript`、`unlocker`、`sighash`、`taproot` 等。
- 文档、示例、测试数据等非运行时代码放在 `docs/`、`examples/`、`testing/` 等专门目录中。

---

## 导入与基本使用示例

```go
import (
    bt "github.com/sCrypt-Inc/go-bt/v2"           // 主包（本仓库根目录）
    "github.com/sCrypt-Inc/go-bt/v2/unlocker"     // 解锁器实现
    "github.com/sCrypt-Inc/go-bt/v2/bscript"      // 脚本与地址相关
)

// 构造交易
tx := bt.NewTx()
tx.From("txid", 0, "script", 1000)
tx.PayToAddress("address", 500)

// 使用解锁器完成签名
u := &unlocker.Simple{PrivateKey: privKey}
if err := tx.FillAllInputs(ctx, u); err != nil {
    panic(err)
}

// 使用 bscript 生成或解析脚本/地址
addr, err := bscript.NewAddressFromString("address")
if err != nil {
    panic(err)
}
_ = addr
```

# tbc-lib-go 文件结构说明

## 最终目录结构

```
tbc-lib-go/
├── 根目录 (package bt)
│   ├── 交易核心模块
│   │   ├── tx.go                    # 交易主结构
│   │   ├── input.go                 # 交易输入
│   │   ├── output.go                # 交易输出
│   │   ├── txinput.go               # 输入构建方法
│   │   ├── txoutput.go              # 输出构建方法
│   │   ├── txchange.go              # 找零处理
│   │   ├── txjson.go                # JSON 序列化
│   │   ├── txjson_node.go           # 节点格式 JSON
│   │   └── *_test.go                # 测试文件
│   │
│   ├── 签名模块
│   │   ├── signaturehash.go          # 签名哈希计算
│   │   └── signaturehash_test.go
│   │
│   ├── 费用模块
│   │   ├── fees.go                   # 费用计算
│   │   └── fees_test.go
│   │
│   ├── UTXO 模块
│   │   ├── utxo.go                   # UTXO 结构
│   │   ├── utxojson.go               # UTXO JSON 处理
│   │   └── utxojson_test.go
│   │
│   ├── 工具模块
│   │   ├── varint.go                 # VarInt 编码
│   │   ├── bytemanipulation.go       # 字节操作
│   │   ├── consts.go                 # 常量定义
│   │   ├── errors.go                 # 错误定义
│   │   ├── docs.go                   # 文档
│   │   └── *_test.go
│   │
│   └── unlocker.go                   # 解锁器接口定义
│
├── bscript/                          # 脚本模块 (package bscript)
│   ├── script.go
│   ├── address.go
│   ├── interpreter/
│   └── ...
│
├── unlocker/                         # 解锁器实现 (package unlocker)
│   ├── simple.go
│   └── simple_test.go
│
├── sighash/                          # 签名哈希标志 (package sighash)
│   └── flag.go
│
└── examples/                         # 示例代码
    └── ...
```

## 模块说明

### package bt (主包)
所有核心功能都在根目录的 `package bt` 中，包括：

1. **交易核心模块**
   - `tx.go` - 交易主结构体和方法
   - `input.go` / `output.go` - 交易输入输出
   - `txinput.go` / `txoutput.go` - 输入输出构建方法
   - `txchange.go` - 找零处理
   - `txjson.go` / `txjson_node.go` - JSON 序列化

2. **签名模块**
   - `signaturehash.go` - 签名哈希计算（Legacy 和 ForkID）

3. **费用模块**
   - `fees.go` - 费用计算和验证

4. **UTXO 模块**
   - `utxo.go` - UTXO 数据结构
   - `utxojson.go` - UTXO JSON 处理

5. **工具模块**
   - `varint.go` - VarInt 编码
   - `bytemanipulation.go` - 字节操作工具
   - `consts.go` - 常量定义
   - `errors.go` - 错误定义
   - `docs.go` - 包文档

6. **解锁器接口**
   - `unlocker.go` - Unlocker 和 UnlockerGetter 接口定义

### package unlocker (独立包)
解锁器的具体实现：
- `unlocker/simple.go` - 简单的私钥解锁器实现

### package bscript (独立包)
脚本处理相关功能，包括脚本构建、地址处理、脚本解释器等。

### package sighash (独立包)
签名哈希标志定义。

## 文件命名约定

虽然所有 `package bt` 的文件都在根目录，但通过文件命名可以清晰识别模块：

- `tx*.go` - 交易相关
- `*input*.go` - 输入相关
- `*output*.go` - 输出相关
- `*change*.go` - 找零相关
- `*json*.go` - JSON 相关
- `signature*.go` - 签名相关
- `fees*.go` - 费用相关
- `utxo*.go` - UTXO 相关
- `varint*.go` - VarInt 相关
- `*manipulation*.go` - 字节操作相关

## 注意事项

⚠️ **Go 包结构限制**：
- 在 Go 中，同一个包的所有文件必须在同一个目录中
- 因此，所有 `package bt` 的文件都在根目录
- 独立的包（如 `unlocker`, `bscript`）可以有自己的目录

## 使用示例

```go
import (
    "github.com/sCrypt-Inc/go-bt/v2"           // package bt
    "github.com/sCrypt-Inc/go-bt/v2/unlocker"  // package unlocker
    "github.com/sCrypt-Inc/go-bt/v2/bscript"   // package bscript
)

// 使用 bt 包
tx := bt.NewTx()
tx.From("txid", 0, "script", 1000)
tx.PayToAddress("address", 500)

// 使用 unlocker 包
unlocker := &unlocker.Simple{PrivateKey: privKey}
tx.FillAllInputs(ctx, unlocker)
```




