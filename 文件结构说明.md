# tbc-lib-go 文件结构说明

> 本文描述当前 `tbc-lib-go` 仓库的目录与模块划分。自 JS-style layout 重构（分支 `refactor/js-layout`，
> 设计文档 [`docs/superpowers/specs/2026-04-24-js-style-layout-design.md`](docs/superpowers/specs/2026-04-24-js-style-layout-design.md)）
> 起，整体布局已与官方 JavaScript 对应物 [`tbc-lib-js`](https://github.com/TuringBitChain/tbc-lib-js) 的 `lib/`
> 目录**拓扑同构**，并在根包保留一层**门面（facade）**以确保历史 `tbc.X` API 零改动兼容。

## 顶层目录概览

```
tbc-lib-go/                         module github.com/LoongYearMeta/tbc-lib-go
├── docs.go                         根包 package 注释
├── tbc.go                          门面层：re-export 所有历史 tbc.X 符号
├── tbc_facade_test.go              门面完整性 compile-time 自测
│
├── transaction/                    JS lib/transaction/：交易构造、输入/输出、费率、签名哈希、JSON
│   └── sighash/                    JS lib/transaction/sighash.js：SIGHASH flag 常量
│
├── script/                         JS lib/script/：脚本构造、解析、地址、BIP276
│   └── interpreter/                脚本执行引擎（含 debug/ errs/ scriptflag 子包）
│
├── block/                          JS lib/block/：区块、区块头、Merkle 块
├── networks/                       JS lib/networks.js：Livenet / Testnet / Regtest / STN
├── message/                        JS lib/message/：消息签名与验签
├── ecies/                          JS lib/ecies/：ECIES (BIE1) 加解密
├── crypto/                         JS lib/crypto/：Hash / ECDSA / Signature / BN / 随机数
├── encoding/                       JS lib/encoding/：VarInt / Base58 / Base58Check / BufferReader/Writer / Hex
│
├── util/
│   ├── pushmeta/                   OP_PUSH_META outpoint 辅助（TBC 独有）
│   ├── ftunlock/                   FT 合约解锁辅助（TBC 独有）
│   └── partialsha256/              JS lib/util/partial-sha256.js：部分 SHA256 状态
│
├── taproot/                        Taproot 数据结构与对齐辅助（JS 无对应）
├── unlocker/                       私钥解锁器实现（JS 无对应，Go 特有接口实现）
│
├── sighash/                        forwarder：转发到 transaction/sighash
├── bscript/                        forwarder：转发到 script/
│
├── testing/                        测试数据（二进制区块、交易）
├── docs/                           Markdown 文档（本文件 + docs/*.md）
│
└── 其他工程文件                     go.mod / go.sum / Makefile / LICENSE / README.md / CLAUDE.md 等
```

---

## 根包：`package tbc`（门面层）

根包现在只承载**门面（facade）**职责：`tbc.go` 通过类型别名 / 变量别名 / 常量 / 错误 sentinel
re-export 的方式把子包里的公开符号原样挂回根包。历史上所有写 `tbc.Tx` / `tbc.NewTx()` /
`tbc.Livenet` / `tbc.ErrNoUTXO` 的下游代码**零改动**仍可工作。

### 1. `docs.go`

根包的 GoDoc 入口，仅保留 package 级注释。

### 2. `tbc.go`（门面）

按功能域分 section re-export。分类如下：

- **`Network` / 预定义网络 / `AddNetwork` / `GetNetwork` / `RemoveNetwork`** ← `networks/`
- **`DefaultNetwork`**：**本地定义**（`var DefaultNetwork = Livenet`），**不是** re-export。
  原因见下方说明。
- **`VarInt` / `NewVarIntFromBytes` / `ReverseBytes` / `IsHexString` 等** ← `encoding/`
- **`Tx` / `Input` / `Output` / `UTXO` / `FeeQuote` / `Unlocker` 等类型别名**
  + **`NewTx` / `NewTxFromString` / `NewFeeQuote` / `IsValidTxID` 等构造器**
  + **`DustLimit` / `FeeTypeStandard` 等常量**
  + **`ErrInvalidTxID` / `ErrNoUTXO` / `ErrInsufficientFunds` 等 sentinel** ← `transaction/`
- **`Block` / `BlockHeader` / `MerkleBlock` + 构造器 + 区块相关常量与 sentinel** ← `block/`
- **`Message` + `SignMessage` / `VerifyMessage` 等** ← `message/`
- **`ECIES` / `ECIESOptions` / `NewECIES` / `EncryptFor` / `DecryptWith` / `RandomBytes`** ← `ecies/`
- **`CurrentInputOutpointBytes`** ← `util/pushmeta/`
- **`GetPreTxdata` / `GetPrePreTxdata` / `GetCurrentTxdata` 等 FT 解锁辅助** ← `util/ftunlock/`

> **关于 `DefaultNetwork`**：`var = networks.DefaultNetwork` 只能得到**拷贝**，下游赋值
> `tbc.DefaultNetwork = tbc.Testnet` 不会影响 `networks` 子包内的值，语义会静默回退。
> 因此 `DefaultNetwork` **只在根包 `tbc.go` 一处定义**，`networks/` 不再独立持有。
> 详见设计文档 §5.6。

### 3. `tbc_facade_test.go`

`package tbc_test` 下的编译期自测，把门面里每个符号各触碰一次；只要能编译通过即表示门面完整无缺失。

---

## 子包详细说明

### `transaction/`（对应 JS `lib/transaction/`）

交易层的核心，约 29 个 `.go` 文件（含 `_test`）：

- `transaction.go` / `transaction_test.go` — `Tx` 结构、构造、序列化、`NewTxFromString/Bytes/Stream`
- `input.go` / `input_build.go` — 交易输入与 `From` / `FromUTXOs` / `FromChain` 构建方法
- `output.go` / `output_build.go` — 交易输出与 `PayToAddress` / `To` / `ToMultiple`
- `change.go` — 找零与 `Change(addr, feeQuote)`
- `fees.go` — `FeeQuote` / `FeeQuotes` / `CeilMiningFeeFromEstimatedBytes`
- `transaction_json.go` / `transaction_json_node.go` — JSON / node-style JSON 序列化
- `js_parity.go` / `js_parity_test.go` — 与 `tbc-lib-js` `_estimateSize` / `_estimateFee` 对齐的
  parity 助手与金标测试
- `unspentoutput.go` / `unspentoutput_json.go` — `UTXO` 数据结构与 JSON
- `signature_hash.go` — 签名哈希算法（Legacy + ForkID）
- `unlocker.go` — `Unlocker` / `UnlockerGetter` / `UnlockerParams` 接口定义
- `consts.go` — `DustLimit` / `MaxTxInSequenceNum` / `FeeType*` 常量
- `errors.go` — `ErrInvalidTxID` / `ErrNoUTXO` / `ErrInsufficientFunds` 等 sentinel 集中
- `sighash/flag.go` — `SIGHASH_ALL` / `SIGHASH_FORKID` 等标志位（JS `lib/transaction/sighash.js`）

### `script/`（对应 JS `lib/script/`）

从原 `bscript/` 改名而来；包名也是 `script`：

- `script.go` — 核心 `Script` 类型
- `address.go` / `addressvalidation.go` — 地址生成 / 解析 / 校验
- `bip276.go` — BIP276 可读脚本表示
- `unlockingscript.go` — 解锁脚本构造
- `opcodes.go` / `oppushdata.go` — 操作码常量与 pushdata
- `errors.go` / `doc.go` — 包内错误与文档
- `interpreter/` — 脚本解释器（`engine.go` / `operations.go` / `opcodeparser.go` / `stack.go` /
  `thread.go` / `state.go` 等），含 `debug/` / `errs/` / `scriptflag/` 三个子包

### `block/`（对应 JS `lib/block/`）

- `block.go` — `Block{Header, Transactions}` 结构与反序列化
- `blockheader.go` — `BlockHeader` 字段、默克尔校验、难度相关
- `merkleblock.go` — Merkle Block（SPV）
- `errors.go` — 区块层 sentinel 集中

### `networks/`（对应 JS `lib/networks.js`）

Go 侧用复数避免与标准库 `net/` 冲突：

- `networks.go` — `Network` 类型 + `Livenet` / `Testnet` / `Regtest` / `STN` 实例 +
  `AddNetwork` / `GetNetwork` / `RemoveNetwork`

### `message/`、`ecies/`

- `message/message.go` — `SignMessage` / `VerifyMessage[WithPubKey/WithAddress]` + 多种输入适配器
- `ecies/ecies.go` — `ECIES` / `ECIESOptions` / `NewECIES` / `EncryptFor` / `DecryptWith` / `RandomBytes`

### `crypto/`（对应 JS `lib/crypto/`）

薄壳 re-export 层，把 `libsv/go-bk/{bec, crypto}` 等外部库按 JS 的分组结构暴露出来：

- `hash.go` — Hash160 / Sha256 / Sha256d / Ripemd160
- `ecdsa.go` — 签名 / 验签入口
- `point.go` / `signature.go` — 椭圆曲线点与 `Signature` 类型
- `bn.go` — 大数（secp256k1 / `math/big`）
- `random.go` — `crypto/rand` 便捷封装

### `encoding/`（对应 JS `lib/encoding/`）

- `varint.go` — VarInt 编解码
- `bytes.go` — `ReverseBytes` / `LittleEndianBytes` / `ReadVarBytes` / `WriteVarBytes`
- `base58.go` / `base58check.go` — Base58 / Base58Check
- `bufferreader.go` / `bufferwriter.go` — 流式读写
- `hex.go` — `IsHexString` 等 hex 工具

### `util/`

TBC 独有或通用工具：

- `util/pushmeta/` — `CurrentInputOutpointBytes`：OP_PUSH_META outpoint 40 字节
- `util/ftunlock/` — FT v1 / v2 / coin 合约预映像与 partial-SHA256 偏移常量
- `util/partialsha256/` — 部分 SHA256 状态（对应 JS `lib/util/partial-sha256.js`）

### `taproot/`、`unlocker/`

- `taproot/` — Taproot 数据结构与对齐测试；JS 无对应
- `unlocker/` — `unlocker.Simple` / `unlocker.Getter` 基于私钥的 `Unlocker` 实现；JS 无对应

### Forwarder 包：`sighash/`、`bscript/`

这两个包**仅为保持老 import path 可用**，内部是薄薄一层别名转发：

- `sighash/flag.go` — `type Flag = transaction/sighash.Flag`，所有常量以 `var` 别名挂回
- `bscript/forwarder.go` — 把 `script/` 里的公开类型 / 常量 / 函数 alias 回 `bscript.*`

新代码建议直接 import `transaction/sighash` 或 `script`；老代码完全不用改。

### `testing/`、`docs/`、`cmd/*`（占位）

- `testing/data/` — 二进制测试数据（区块、交易原始字节）
- `docs/` — Markdown 文档入口与分主题页（`index.md` / `transaction.md` / `block.md` /
  `script.md` / `networks.md` / `unspentoutput.md` / `ecies.md`）
- 本仓库不再内置 HTTP 索引器客户端，`cmd/` 与 `examples/` 已于 `814aa53` 随之移除；
  下游如需节点交互可使用 `github.com/sCrypt-Inc/tbc-contract-go/lib/api`

---

## 两种 import 风格

### 风格 A：门面（推荐，兼容历史代码）

```go
import (
    "context"

    "github.com/libsv/go-bk/bec"
    tbc "github.com/LoongYearMeta/tbc-lib-go"
    "github.com/LoongYearMeta/tbc-lib-go/unlocker"
)

func example(utxo *tbc.UTXO, priv *bec.PrivateKey, to, change string) {
    tx := tbc.NewTx().FromChain(utxo).To(to, 50_000).Change(change, nil)
    tx.Sign(context.Background(), &unlocker.Getter{PrivateKey: priv})
}
```

### 风格 B：直接按子包 import（更细粒度）

```go
import (
    "github.com/LoongYearMeta/tbc-lib-go/block"
    "github.com/LoongYearMeta/tbc-lib-go/transaction"
    "github.com/LoongYearMeta/tbc-lib-go/script"
)

tx := transaction.NewTx()
blk, _ := block.NewBlockFromString(hex)
s, _ := script.NewFromASM("OP_DUP OP_HASH160 …")
_, _, _ = tx, blk, s
```

两种风格可以**混用**：`tbc.Tx == transaction.Tx`（因为是 `type Tx = transaction.Tx` alias）。

---

## 如何新增 API

1. 确定它在 JS `lib/` 下属于哪个分组（`transaction/` / `script/` / `block/` / `crypto/` /
   `encoding/` / `ecies/` / `message/` / `networks.js` / `util/`）
2. 在对应 Go 子包里实现（新增文件或追加到已有文件），测试同目录 `*_test.go`
3. **如果需要让老代码 `tbc.X` 继续可用**，在根包 `tbc.go` 加一行 alias 或 re-export：
   - 类型：`type X = subpkg.X`
   - 函数 / 构造器：`var X = subpkg.X`
   - 常量：`const X = subpkg.X`
   - 错误 sentinel：`var ErrX = subpkg.ErrX`
4. 跑 `go build ./... && go vet ./... && go test -count=1 ./...`
5. 如在公开 API 面上改动，`tbc_facade_test.go` 里追加一次符号引用，保护门面完整性
6. 相关主题的 `docs/*.md` 同步示例

---

## 参考

- 设计文档：[`docs/superpowers/specs/2026-04-24-js-style-layout-design.md`](docs/superpowers/specs/2026-04-24-js-style-layout-design.md)
- JS 对应物：[`tbc-lib-js`](https://github.com/TuringBitChain/tbc-lib-js) 的 `lib/` 目录
- 主题文档：`docs/index.md` 及各主题 md
