# IDE and os files
.history
.idea/*
.vscode/*
.DS_Store
*~

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# linter binary
golangci-lint

# Notes
todo.md

# Distribution directory for releases
dist

# Build output (make build-go / make lint cache binaries here)
bin/

# Coverage
coverage.txt
coverage.html

# Go test binaries / profiling dumps
*.test
*.prof
*.pprof

# vim
*.swp

# Go module / vendor caches
.gomodcache/
vendor/

# Local secrets
.env
.env.*
!.env.example

# AI assistant local state (CLAUDE.md is tracked; per-user settings are not)
.claude/
.aider*
CLAUDE.md
docs/superpowers/
